// using this instead of app.js as it was already in use for browserify.. should have done broserify in a seperate folder
// here we do the things of app.js and this is the on in index.html present

define([
    './amdmodule'
], function (amdmodule) {});